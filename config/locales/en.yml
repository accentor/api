# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# The following keys must be escaped otherwise they will not be retrieved by
# the default I18n backend:
#
# true, false, on, off, yes, no
#
# Instead, surround them with single quotes.
#
# en:
#   'true': 'foo'
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  activerecord:
    errors:
      models:
        album:
          attributes:
            title: 
              blank: "albums.title-blank"
        album_artists:
          attributes:
            name:
              blank: "aa.name-blank"
            order:
              blank: "aa.order-blank"
        album_label:
          attributes:
            catalogue_number: 
              blank: "al.cat-blank"
        artist:
          attributes:
            name:
              blank: "artists.name-blank"
        codec:
          attributes:
            mimetype: 
              blank: "codec.mime-blank"
            extension:
              blank: "codec.ext-blank"
              taken: "codec.ext-taken"
        codec_conversion:
          attributes:
            name:
              blank: "codecconv.nane-blank"
              taken: "codecconv.name-taken"
            ffmpeg_params:
              blank: "codecconv.ffmpeg-blank"
            resulting_codec:
              blank: "codecconv.result-blank"
        genre:
          attributes:
            name:
              blank: "genre.name-blank"
              taken: "genre.name-taken"
        image_type:
          attributes:
            mimetype: 
              blank: "image.mime-blank"
            extension:
              blank: "image.ext-blank"
              taken: "image.ext-taken"
        label:
          attributes: 
            name: 
              blank: "label.name-blank"
        location:
          attributes:
            path:
              blank: "location.path-blank"
              taken: "location.path-taken"
        track:
          attributes:
            title:
              blank: "tracks.title-blank"
            number:
              blank: "tracks.number-blank"
        track_artist:
          attributes:
            artist:
              blank: "ta.artist-blank"
            name:
              blank: "ta.name-blank"
              taken: "ta.name-taken"
            order:
              blank: "ta.order-blank"
            role:
              blank: "ta.role-blank"
            track:
              blank: "ta.track-blank"
        user:
          attributes:
            name:
              blank: "user.name-blank"
            password_digest:
              blank: "user.password-blank"
            permission:
              blank: "user.permission-blank"
  auth_tokens:
    create:
      wrong_credentials: 'user.wrong-credentials'
  users:
      current_password_is_incorrect: 'user.current-password-incorrect'
  pundit:
    default: 'not-authorized'
